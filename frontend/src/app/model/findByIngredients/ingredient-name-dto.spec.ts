import { IngredientNameDTO } from './ingredient-name-dto';

describe('IngredientNameDTO', () => {
  it('should create an instance', () => {
    expect(new IngredientNameDTO()).toBeTruthy();
  });
});
