import { IngredientRecipeDTO } from './ingredient-recipe-dto';

describe('IngredientRecipeDTO', () => {
  it('should create an instance', () => {
    expect(new IngredientRecipeDTO()).toBeTruthy();
  });
});
