import { CookingStepRecipeDTO } from './cooking-step-recipe-dto';

describe('CookingStepRecipeDTO', () => {
  it('should create an instance', () => {
    expect(new CookingStepRecipeDTO()).toBeTruthy();
  });
});
