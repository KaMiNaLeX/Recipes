import { CookingDifficultyDTO } from './cooking-difficulty-dto';

describe('CookingDifficultyDTO', () => {
  it('should create an instance', () => {
    expect(new CookingDifficultyDTO()).toBeTruthy();
  });
});
